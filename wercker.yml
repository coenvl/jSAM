box: qlik/gradle
build:
  steps:
    - script:
        name: show info
        code: |
          echo "Starting display of versions"
          which gradle
          gradle -v
    - script:
        name: build
        code: |
          echo "Starting gradle test"
          gradle build test

