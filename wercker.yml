box: 
  id: niaquinto/gradle
  entrypoint: /bin/bash -c
build:
  steps:
    - script:
        name: Build java code
        code: |
          gradle build
    - script:
        name: Run Junit Tests
        code: |
          gradle test